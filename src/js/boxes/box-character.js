
// photoshop.box.character

{
	toggle(el, state) {
		if (state === "on") {
			
		} else {

		}
	},
	dispatch(event) {
		let root = photoshop,
			self = root.box.character;

		switch (event.type) {
			case "type-of-event":
				break;
		}
	}
}
