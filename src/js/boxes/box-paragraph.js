
// photoshop.box.paragraph

{
	toggle(el, state) {
		if (state === "on") {
			
		} else {

		}
	},
	dispatch(event) {
		let root = photoshop,
			self = root.box.paragraph;

		switch (event.type) {
			case "type-of-event":
				break;
		}
	}
}
