
// photoshop.box.layers

{
	toggle(el, state) {
		if (state === "on") {
			
		} else {

		}
	},
	dispatch(event) {
		let root = photoshop,
			self = root.box.layers;

		switch (event.type) {
			case "type-of-event":
				break;
		}
	}
}
