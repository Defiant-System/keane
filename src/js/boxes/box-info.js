
// photoshop.box.info

{
	toggle(el, state) {
		if (state === "on") {
			
		} else {

		}
	},
	dispatch(event) {
		let root = photoshop,
			self = root.box.info;

		switch (event.type) {
			case "type-of-event":
				break;
		}
	}
}
