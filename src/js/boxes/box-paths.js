
// photoshop.box.paths

{
	toggle(el, state) {
		if (state === "on") {
			
		} else {

		}
	},
	dispatch(event) {
		let root = photoshop,
			self = root.box.paths;

		switch (event.type) {
			case "type-of-event":
				break;
		}
	}
}
