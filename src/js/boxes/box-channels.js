
// photoshop.box.channels

{
	toggle(el, state) {
		if (state === "on") {
			
		} else {

		}
	},
	dispatch(event) {
		let root = photoshop,
			self = root.box.channels;

		switch (event.type) {
			case "type-of-event":
				break;
		}
	}
}
