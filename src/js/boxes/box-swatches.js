
// photoshop.box.swatches

{
	toggle(el, state) {
		if (state === "on") {
			
		} else {

		}
	},
	dispatch(event) {
		let root = photoshop,
			self = root.box.swatches;

		switch (event.type) {
			case "type-of-event":
				break;
		}
	}
}
