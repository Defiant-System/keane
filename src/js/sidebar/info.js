
// keane.sidebar.info

{
	init() {

	},
	dispatch(event) {
		let APP = keane,
			Self = APP.sidebar.info,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "show-color-values":
				break;
		}
	}
}
