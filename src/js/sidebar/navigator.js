
// keane.sidebar.navigator

{
	init() {

	},
	dispatch(event) {
		let APP = keane,
			Self = APP.sidebar.navigator,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "show-color-values":
				break;
		}
	}
}
