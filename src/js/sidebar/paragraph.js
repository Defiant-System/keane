
// keane.sidebar.paragraph

{
	init() {

	},
	dispatch(event) {
		let APP = keane,
			Self = APP.sidebar.paragraph,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "show-color-values":
				break;
		}
	}
}
