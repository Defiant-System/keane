
// keane.sidebar.characters

{
	init() {

	},
	dispatch(event) {
		let APP = keane,
			Self = APP.sidebar.characters,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "show-color-values":
				break;
		}
	}
}
