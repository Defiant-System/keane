
// keane.sidebar.swatches

{
	init() {

	},
	dispatch(event) {
		let APP = keane,
			Self = APP.sidebar.swatches,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "show-color-values":
				break;
		}
	}
}
