
// keane.sidebar.channels

{
	init() {

	},
	dispatch(event) {
		let APP = keane,
			Self = APP.sidebar.channels,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "show-color-values":
				break;
		}
	}
}
