
// keane.sidebar.layers

{
	init() {

	},
	dispatch(event) {
		let APP = keane,
			Self = APP.sidebar.layers,
			el;
		// console.log(event);
		switch (event.type) {
			// custom events
			case "show-color-values":
				break;
		}
	}
}
